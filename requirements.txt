# Minimum versions for reconstructing the documented workflow. The historical
# 2022-era run was not captured with a fully pinned environment, so these are
# not an exact lockfile and numerical reproduction is not guaranteed.
torch>=2.0.0
diffusers>=0.21.0
transformers>=4.30.0
accelerate>=0.20.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
Pillow>=10.0.0
tqdm>=4.65.0
